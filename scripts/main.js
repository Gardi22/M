// Simulating visitor count
document.addEventListener("DOMContentLoaded", function () {
  const visitorCount = 12345; // Example static number
  document.getElementById("visitorCount").textContent = visitorCount;
});
